# TickView 设计与实现方案

> 桌面极简看盘小工具：实时获取指定 A 股 / ETF 最新价格并显示，支持价格预警提醒，界面尽可能小而隐蔽。
> 跨平台：**Windows / macOS / Linux**。
>
> 创建日期：2026-06-04 · 最近更新：2026-06-04 · 技术栈：Python 3.9+ / Tkinter / requests

---

## 1. 需求回顾

| 维度 | 决策 |
|---|---|
| 界面形态 | 迷你悬浮条（无边框、置顶可开关、半透明、可拖动、可秒隐藏） |
| 显示内容 | 只显示**最新价 + 涨跌幅**（不显示代码），红涨绿跌；单击行看当日最高/最低 |
| 标的范围 | A 股主板/创业板/科创板/北交所 + 沪深 **ETF/LOF/基金** + **指数**（上证指数等） |
| 价格精度 | 股票 2 位小数，基金/ETF 3 位小数（按代码自动判定） |
| 多股展示 | 单行轮播 / 纵向列表 两种模式可切换；列表内可拖动排序 |
| 预警方式 | 触发股票背景**闪烁/变色**（静默，无声、无系统弹窗） |
| 配置方式 | 右键菜单 + 设置小窗，配置存本地 JSON |
| 技术栈 | Python + Tkinter；PyInstaller 打包（Windows exe / macOS .app / Linux 可执行） |
| 数据源 | 三源容灾：腾讯（主）/ 新浪 / 东财（免费、无 key） |

设计目标：**小、静、稳**。绝不能有异常弹窗、声音或显眼提示。

---

## 2. 总体架构

单进程 Python 应用，模块职责清晰、可独立测试：

```
TickView/
├── main.py          # 入口：组装各模块、启动主循环、后台行情线程
├── quote.py         # 行情模块：多源容灾拉取(腾讯/新浪/东财)、解析、代码规范化、交易时段判断
├── config.py        # 配置模块：本地 JSON 读写、校验、损坏回退
├── ui.py            # 界面模块：悬浮条 + 设置窗 + 右键菜单 + 预警闪烁
├── requirements.txt # 仅 requests
├── TickView.spec    # PyInstaller 打包配置（Windows/macOS 共用，按平台分支）
├── build_win.bat    # Windows 打包脚本（一键出 exe，用项目内 upx 压缩）
├── build_mac.sh     # macOS 打包脚本（一键出 .app）
├── build_linux.sh   # Linux 打包脚本（一键出单文件可执行）
├── upx/             # 内置 UPX 压缩器（Windows 打包用）
├── assets/          # 应用图标 icon.ico/.icns/.png + 生成脚本 make_icon.py
├── README.md        # 使用说明 / 软件介绍 / 打包说明
└── DESIGN.md        # 本文档
```

### 线程模型

```
┌─────────────────┐   poke()    ┌──────────────────────┐
│  UI 线程 (Tk)    │ ──────────▶ │ QuoteWorker 后台线程   │
│  TickViewApp     │             │  - 定时拉取行情(多源)   │
│  - 事件循环       │ ◀────────── │  - 交易时段 3s/盘后 60s │
│  - after() 轮询   │   queue     │  - 失败静默重试         │
└─────────────────┘  (quotes/    └──────────────────────┘
                      error)
```

- 主线程跑 Tkinter 事件循环；后台单线程拉行情。
- 跨线程通过 `queue.Queue` 单向传递，UI 用 `root.after()` 周期性消费——**Tkinter 非线程安全，行情数据绝不在后台线程直接碰控件**。
- 配置变更（增删股票）时 UI 调 `worker.poke()`，后台线程立即拉一次，无需等下个周期。

---

## 3. 数据源：三源容灾

按优先级 **腾讯 → 新浪 → 东财** 依次尝试，第一个返回非空数据的即采用。**每个刷新周期都从头尝试**，因此主源（腾讯）恢复后会自动切回，无需手动干预。三源全失败才抛 `AllSourcesError`（界面静默变灰）。源切换时记一条 INFO 日志。

| 优先级 | 数据源 | 端点 | 格式 | 说明 |
|---|---|---|---|---|
| 1（主） | 腾讯 | `qt.gtimg.cn/q=` | GBK，`~`分隔 | 批量、字段全（含涨跌幅） |
| 2（备） | 新浪 | `hq.sinajs.cn/list=` | GBK，逗号分隔 | 批量、需 Referer；涨跌幅自算 |
| 3（备） | 东财 | `push2.eastmoney.com/api/qt/stock/get` | JSON | 逐只请求（批量接口字段不稳定），价格按 `f59` 小数位还原 |

- 每个源是一个纯函数 `fetch(codes, timeout) -> {code: Quote}`，注册在 `quote.SOURCES`，易于独立测试与增删。
- 统一经 `_make_quote()` 构造：停牌价为 0 用昨收兜底，涨跌幅缺省时用 `(现价-昨收)/昨收` 计算。
- 同时解析**当日最高/最低**（腾讯 `[33]/[34]`、新浪 `[4]/[5]`、东财 `f44/f45`），存入 `Quote.high/low`，供点击行时查看；缺失记 0。
- 单源超时 4s；解析容错：字段不足/数值异常 → 丢弃该条或降级到下一个源。
- 东财 secid 前缀：沪 `1.`、深 `0.`、北交所 `0.`。

### 刷新策略

| 时段 | 间隔 |
|---|---|
| 交易时段（工作日 9:15–11:30 / 13:00–15:00） | 3 秒 |
| 非交易时段 | 60 秒（仅为启动时显示收盘价） |

网络失败：**不弹任何提示**，界面价格整体变灰（stale），后台静默重试；只在「成功↔失败」状态切换时记一条日志，避免刷屏。

---

## 4. 悬浮条界面

- 无边框 `overrideredirect(True)`、置顶 `-topmost`（**可在设置中开关**）、透明度 `-alpha` 可调（0.3–1.0）。
- **置顶开关**：`always_on_top` 配置项（默认开）。开启时窗口始终最前显示，并周期性重申置顶防止被抢占；关闭后表现为普通窗口，可被其他窗口遮挡。设置窗复选框实时切换。
- 深灰底等宽字体（Windows `Consolas` / macOS `Menlo` / 其他 `monospace`），每行显示 `最新价 涨跌幅`（**不含代码**），**红涨绿跌**（A 股习惯）。
- **价格小数位自适应**：`quote.price_decimals()` 按代码判定——沪市 `5xxxxx`、深市 `1xxxxx` 为基金/ETF/LOF，显示 3 位；其余股票 2 位。按代码而非数据源字符串判定，源切换时位数不抖动。
- 行宽固定（`ROW_WIDTH=16` 字符），轮播切换时窗口尺寸稳定不抖动。

### 两种显示模式

- **单行轮播（默认）**：始终一行，多只股票按 `carousel_interval` 自动切换；滚轮可手动切。
- **纵向列表**：每只一行，全部可见，窗口随数量变高。

### 交互

| 操作 | 行为 |
|---|---|
| 拖动 | 移动窗口，位置记忆到配置（用 5px 阈值区分点击/拖动） |
| 双击 | 隐藏成屏幕原位 6×6 像素小点，点小点恢复 |
| 单击某行 | 确认并消除该行预警；并在**悬浮条正下方**弹出气泡显示**当日最高/最低**，**常驻显示**，再次点该行收起（点别的行则切换；拖动/隐藏/布局变化时自动收起。统一贴窗口底部而非被点行下方，多行时不被遮挡） |
| 右键 | 在**窗口正下方**弹出菜单：设置 / 切换显示模式 / 隐藏 / 退出（macOS 上副键常为 `Button-2`，另支持 `Control+点按`） |
| 滚轮 | 轮播模式下手动切换（Windows/macOS 用 `delta`，Linux 用 `Button-4/5`） |

> **退出**：菜单退出走 `_quit()`——先 `unpost` 收起菜单、`withdraw` 同步隐藏窗口、刷新屏幕区域后再 `destroy`。不能用 `after_idle` 延迟销毁，否则 Windows/macOS 上事件循环会停在等待输入，导致窗口要等下次点击才消失。

---

## 5. 预警机制

每只股票可设**上限价**、**下限价**（可只设一边，留空为不预警）。

### 状态机（每只股票独立）

```
{upper_armed, lower_armed, active}
```

- **武装（armed）**：价格回到区间内 → 重新武装，下次越界可再次触发。
- **触发（active=True）**：`armed 且 价格越界` → 进入闪烁。背景在暗红/暗黄间 1 秒周期交替（静默，无声）。
- **轮播暂停**：有任意预警 active 时，轮播停在触发的那只股票上。
- **确认（点击该行）**：`active=False` 熄灭；若此刻价格仍越界，则解除该方向武装，**价格回到区间内之前不再重复触发**（避免反复打扰）。
- 修改阈值或删除股票时重置该股票预警状态。

这套设计保证：**越界提醒一次 → 确认后安静 → 回到区间 → 再越界再提醒**，既不漏报也不骚扰。

---

## 6. 设置窗口

Tkinter `Toplevel` 小窗，置顶、不可缩放：

- **股票**：列表展示（**6 位代码**去前缀 + 名称 + 上下限）；输入框加股票（`600519` / `sh600519` / ETF `159206` 自动识别市场）；删除选中；**在列表内按住条目上下拖动即可调整顺序**（拖动用 `pop`+`insert` 移动，松手才持久化并同步悬浮条顺序；订阅代码不变不重新拉取）；输入框后紧跟 **`?` 按钮**弹出代码规则说明（常量 `_ADD_HELP`）。
- **预警价**：选中股票后填上/下限，「应用」保存；校验为正数、上限 > 下限。
- **显示**：透明度滑块（实时预览，松手保存）、模式单选、轮播间隔、**窗口始终置顶复选框**。

代码市场前缀自动识别规则（`quote.normalize_code`，已带 `sh/sz/bj` 前缀则直接采用）：

| 首位数字 | 市场 / 品种 | 前缀 |
|---|---|---|
| 6 / 9 | 沪市主板 / 科创板 / 沪B | `sh` |
| 5 | 沪市基金 / ETF / LOF（50/51/52/56/58…） | `sh` |
| 0 / 3 | 深市主板 / 创业板 | `sz` |
| 1 | 深市基金 / ETF / LOF（15/16/18…） | `sz` |
| 4 / 8 | 北交所 | `bj` |

**指数**：腾讯/新浪均支持，但**沪市指数代码与深市股票数字冲突**（如 `000001` 既是上证指数 `sh000001` 也是平安银行 `sz000001`），故指数须带前缀：上证指数 `sh000001`、深证成指 `sz399001`、创业板指 `sz399006`、沪深300 `sh000300`。`quote._ALIASES` 额外接受通达信代码 `1A0001` 直达上证指数。

---

## 7. 配置与持久化

- 路径：Windows `%APPDATA%\TickView\config.json`；macOS `~/Library/Application Support/TickView/config.json`；Linux `~/.config/TickView/config.json`。
- 字段：`stocks`（含 code/upper/lower）、`opacity`、`mode`、`carousel_interval`、`always_on_top`、`window_x/y`。
- **健壮性**：
  - 逐字段校验，非法字段回退默认值（不让坏配置导致崩溃）。
  - JSON 损坏 → 备份为 `config.json.bak` 并回退默认。
  - 原子写入（先写 `.tmp` 再 `replace`），避免写一半损坏。
- 日志：`tickview.log`（RotatingFileHandler，512KB × 2 个文件），INFO 级别起（含 WARNING/ERROR）。

---

## 8. 交付与运行

> 详细的安装/使用/打包步骤见 **README.md**，本节只列设计要点。

### 源码调试（任意平台）

```bash
pip3 install -r requirements.txt
python3 main.py
```

### 打包（PyInstaller，统一从 `TickView.spec` 构建）

`TickView.spec` 按平台分支，三个脚本都执行 `pyinstaller TickView.spec`：

| 平台 | 脚本 | 产物 | 要点 |
|---|---|---|---|
| Windows | `build_win.bat` | `dist\TickView.exe`（单文件、无控制台） | 用项目内 `upx\` 压缩；打包后刷新图标缓存 |
| macOS | `build_mac.sh` | `dist/TickView.app` | `BUNDLE` + `LSUIElement`（不在程序坞显示） |
| Linux | `build_linux.sh` | `dist/TickView`（单文件可执行） | 需先装 `python3-tk`；UPX 可选 |

图标：`spec` 按平台引用 `assets/icon.ico`（Windows）/ `assets/icon.icns`（macOS），Linux 可执行文件不内嵌图标，缺失则回退默认。图标由 `assets/make_icon.py`（Pillow）生成，仅重做图标时需要，打包/运行不依赖。

体积优化（`spec`）：`optimize=2`（去 docstring/assert）、`excludes` 排除无用标准库、Windows/Linux 启用 UPX。
**macOS 禁用 UPX**——压缩后的二进制会被 Gatekeeper 拦截、在 Apple Silicon 上崩溃。
Windows 打包：`--onefile` 下 UPX 压的是内部 DLL（外层 exe 无 UPX 段标记属正常），实测约 12MB。
**PyInstaller 产物不跨平台**：各平台需在对应系统上构建。

---

## 9. 验证记录（2026-06-04）

| 项目 | 结果 |
|---|---|
| 4 模块编译 | ✅ |
| 实时行情拉取 | ✅ 茅台/宁德/平安银行 盘中实价 |
| `normalize_code`（含科创板/北交所/**沪深 ETF/LOF**/非法输入） | ✅ |
| ETF 支持 + 价格小数位（`159206→sz159206` 3 位、股票 2 位） | ✅ 实测腾讯源返回卫星ETF 实价 |
| **指数**（`sh000001` / 通达信别名 `1A0001` → 上证指数） | ✅ 实测拉到指数实价 |
| **当日最高/最低**（腾讯/新浪/东财字段解析） | ✅ 实测茅台、卫星ETF 高低值 |
| 配置 5 分支（默认/读写/非法字段/损坏 JSON/空列表） | ✅ |
| UI（预警触发→轮播跳转→确认→不重复、模式切换、隐藏/恢复、断网变灰） | ✅ 真实 Tk 窗口实测 |
| 列表拖动排序 / 退出立即关闭 / 高低气泡 / 菜单与气泡置于窗口下方 | ✅ 逻辑实现（Windows 行为已修） |
| 多源容灾（三源单测 + 腾讯挂→新浪、腾讯+新浪挂→东财、全挂抛异常） | ✅ 实测真实接口 |
| 置顶开关（开启态重申/关闭态不触碰/隐藏恢复跟随开关） | ✅ spy 拦截 attributes 调用验证 |
| Windows 打包（spec + 内置 UPX + 图标内嵌 + 图标缓存刷新） | ✅ 实测出 exe、图标已嵌入 |
| 端到端运行 | ✅ 日志正确记录生效行情源 |
| **macOS / Linux 兼容**（打包 / 字体 / 副键菜单 / 无边框置顶 / 配置目录） | ⚠️ 代码已适配，**待在真机验证** |

---

## 10. 后续可扩展点（YAGNI，暂未实现）

- 全局热键唤出/隐藏（跨平台实现有坑，当前用角落小点替代）。
- 分时图 / 简易 K 线 tooltip。
- 预警历史记录与导出。
- 港股 / 美股行情（当前仅 A 股 + 沪深基金/ETF + 指数）。
- 上海黄金交易所现货（黄金 AU9999 等）：需放宽代码格式 + 符号映射 + 独立解析 SGE 字段。
- macOS `.app` 代码签名 / 公证（免去首次「无法验证开发者」提示）。
